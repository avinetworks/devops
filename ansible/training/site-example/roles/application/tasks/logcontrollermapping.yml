# Automatically Generated File
# Copywrite Avi Networks

  - name: Avi LogControllerMapping | Setup LogControllerMapping
    avi_logcontrollermapping:
      controller: "{{ avi_controller}}"
      username: "{{ avi_username }}"
      password: "{{ avi_password }}"
      tenant: "{{ tenant | default(omit)}}"
      state: "{{item.state | default(omit)}}"
      api_version: "{{ api_version | default(omit)}}"
      name: "{{item.name}}"
      controller_ip: "{{ item.controller_ip | default(omit) }}"
      metrics_mgr_port: "{{ item.metrics_mgr_port | default(omit) }}"
      node_uuid: "{{ item.node_uuid | default(omit) }}"
      prev_controller_ip: "{{ item.prev_controller_ip | default(omit) }}"
      prev_metrics_mgr_port: "{{ item.prev_metrics_mgr_port | default(omit) }}"
      static_mapping: "{{ item.static_mapping | default(omit) }}"
      tenant_ref: "{{ item.tenant_ref | default(omit) }}"
      uuid: "{{ item.uuid | default(omit) }}"
      vs_uuid: "{{ item.vs_uuid | default(omit) }}"
    with_items: "{{ avi_config.logcontrollermapping | default([]) }}"

