# Automatically Generated File
# Copywrite Avi Networks

  - name: Avi ControllerLicense | Setup ControllerLicense
    avi_controllerlicense:
      controller: "{{ avi_controller}}"
      username: "{{ avi_username }}"
      password: "{{ avi_password }}"
      tenant: "{{ tenant | default(omit)}}"
      state: "{{item.state | default(omit)}}"
      api_version: "{{ api_version | default(omit)}}"
      name: "{{item.name}}"
      backend_servers: "{{ item.backend_servers | default(omit) }}"
      cores: "{{ item.cores | default(omit) }}"
      customer_name: "{{ item.customer_name | default(omit) }}"
      license_tier: "{{ item.license_tier | default(omit) }}"
      licenses: "{{ item.licenses | default(omit) }}"
      max_apps: "{{ item.max_apps | default(omit) }}"
      max_ses: "{{ item.max_ses | default(omit) }}"
      max_vses: "{{ item.max_vses | default(omit) }}"
      sockets: "{{ item.sockets | default(omit) }}"
      start_on: "{{ item.start_on | default(omit) }}"
      throughput: "{{ item.throughput | default(omit) }}"
      uuid: "{{ item.uuid | default(omit) }}"
      valid_until: "{{ item.valid_until | default(omit) }}"
    with_items: "{{ avi_config.controllerlicense | default([]) }}"

