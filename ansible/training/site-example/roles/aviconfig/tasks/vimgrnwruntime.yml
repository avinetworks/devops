# Automatically Generated File
# Copywrite Avi Networks

  - name: Avi VIMgrNWRuntime | Setup VIMgrNWRuntime
    avi_vimgrnwruntime:
      controller: "{{ avi_controller}}"
      username: "{{ avi_username }}"
      password: "{{ avi_password }}"
      tenant: "{{ tenant | default(omit)}}"
      state: "{{item.state | default(omit)}}"
      api_version: "{{ api_version | default(omit)}}"
      name: "{{item.name}}"
      apic_vrf_context: "{{ item.apic_vrf_context | default(omit) }}"
      auto_expand: "{{ item.auto_expand | default(omit) }}"
      availability_zone: "{{ item.availability_zone | default(omit) }}"
      cloud_ref: "{{ item.cloud_ref | default(omit) }}"
      datacenter_uuid: "{{ item.datacenter_uuid | default(omit) }}"
      dvs: "{{ item.dvs | default(omit) }}"
      host_refs: "{{ item.host_refs | default(omit) }}"
      interested_nw: "{{ item.interested_nw | default(omit) }}"
      ip_subnet: "{{ item.ip_subnet | default(omit) }}"
      managed_object_id: "{{ item.managed_object_id | default(omit) }}"
      MgmtNW: "{{ item.MgmtNW | default(omit) }}"
      num_ports: "{{ item.num_ports | default(omit) }}"
      switch_name: "{{ item.switch_name | default(omit) }}"
      tenant_name: "{{ item.tenant_name | default(omit) }}"
      tenant_ref: "{{ item.tenant_ref | default(omit) }}"
      type: "{{ item.type | default(omit) }}"
      uuid: "{{ item.uuid | default(omit) }}"
      vlan: "{{ item.vlan | default(omit) }}"
      vlan_range: "{{ item.vlan_range | default(omit) }}"
      vm_refs: "{{ item.vm_refs | default(omit) }}"
      vrf_context_ref: "{{ item.vrf_context_ref | default(omit) }}"
    with_items: "{{ avi_config.vimgrnwruntime | default([]) }}"

