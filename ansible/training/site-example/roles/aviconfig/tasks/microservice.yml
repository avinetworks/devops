# Automatically Generated File
# Copywrite Avi Networks

  - name: Avi MicroService | Setup MicroService
    avi_microservice:
      controller: "{{ avi_controller}}"
      username: "{{ avi_username }}"
      password: "{{ avi_password }}"
      tenant: "{{ tenant | default(omit)}}"
      state: "{{item.state | default(omit)}}"
      api_version: "{{ api_version | default(omit)}}"
      name: "{{item.name}}"
      application_name: "{{ item.application_name | default(omit) }}"
      containers: "{{ item.containers | default(omit) }}"
      created_by: "{{ item.created_by | default(omit) }}"
      description: "{{ item.description | default(omit) }}"
      ip_list: "{{ item.ip_list | default(omit) }}"
      orchestrator_name: "{{ item.orchestrator_name | default(omit) }}"
      tenant_ref: "{{ item.tenant_ref | default(omit) }}"
      uuid: "{{ item.uuid | default(omit) }}"
    with_items: "{{ avi_config.microservice | default([]) }}"

