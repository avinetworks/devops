# Automatically Generated File
# Copywrite Avi Networks

  - name: Avi SecureChannelMapping | Setup SecureChannelMapping
    avi_securechannelmapping:
      controller: "{{ avi_controller}}"
      username: "{{ avi_username }}"
      password: "{{ avi_password }}"
      tenant: "{{ tenant | default(omit)}}"
      state: "{{item.state | default(omit)}}"
      api_version: "{{ api_version | default(omit)}}"
      name: "{{item.name}}"
      ip: "{{ item.ip | default(omit) }}"
      is_controller: "{{ item.is_controller | default(omit) }}"
      local_ip: "{{ item.local_ip | default(omit) }}"
      marked_for_delete: "{{ item.marked_for_delete | default(omit) }}"
      pub_key: "{{ item.pub_key | default(omit) }}"
      pub_key_pem: "{{ item.pub_key_pem | default(omit) }}"
      status: "{{ item.status | default(omit) }}"
      uuid: "{{ item.uuid | default(omit) }}"
    with_items: "{{ avi_config.securechannelmapping | default([]) }}"

