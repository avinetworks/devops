# Automatically Generated File
# Copywrite Avi Networks

  - name: Avi VIMgrVMRuntime | Setup VIMgrVMRuntime
    avi_vimgrvmruntime:
      controller: "{{ avi_controller}}"
      username: "{{ avi_username }}"
      password: "{{ avi_password }}"
      tenant: "{{ tenant | default(omit)}}"
      state: "{{item.state | default(omit)}}"
      api_version: "{{ api_version | default(omit)}}"
      name: "{{item.name}}"
      availability_zone: "{{ item.availability_zone | default(omit) }}"
      cloud_ref: "{{ item.cloud_ref | default(omit) }}"
      connection_state: "{{ item.connection_state | default(omit) }}"
      controller_cluster_uuid: "{{ item.controller_cluster_uuid | default(omit) }}"
      controller_ip_addr: "{{ item.controller_ip_addr | default(omit) }}"
      controller_vm: "{{ item.controller_vm | default(omit) }}"
      cpu_reservation: "{{ item.cpu_reservation | default(omit) }}"
      cpu_shares: "{{ item.cpu_shares | default(omit) }}"
      creation_in_progress: "{{ item.creation_in_progress | default(omit) }}"
      guest_nic: "{{ item.guest_nic | default(omit) }}"
      host: "{{ item.host | default(omit) }}"
      init_vnics: "{{ item.init_vnics | default(omit) }}"
      managed_object_id: "{{ item.managed_object_id | default(omit) }}"
      mem_shares: "{{ item.mem_shares | default(omit) }}"
      memory: "{{ item.memory | default(omit) }}"
      memory_reservation: "{{ item.memory_reservation | default(omit) }}"
      num_cpu: "{{ item.num_cpu | default(omit) }}"
      ovf_avisetype_field: "{{ item.ovf_avisetype_field | default(omit) }}"
      powerstate: "{{ item.powerstate | default(omit) }}"
      se_ver: "{{ item.se_ver | default(omit) }}"
      tenant_ref: "{{ item.tenant_ref | default(omit) }}"
      type: "{{ item.type | default(omit) }}"
      uuid: "{{ item.uuid | default(omit) }}"
      vcenter_datacenter_uuid: "{{ item.vcenter_datacenter_uuid | default(omit) }}"
      vcenter_rm_cookie: "{{ item.vcenter_rm_cookie | default(omit) }}"
      vcenter_se_type: "{{ item.vcenter_se_type | default(omit) }}"
      vcenter_template_vm: "{{ item.vcenter_template_vm | default(omit) }}"
      vcenter_vAppName: "{{ item.vcenter_vAppName | default(omit) }}"
      vcenter_vAppVendor: "{{ item.vcenter_vAppVendor | default(omit) }}"
      vcenter_vm_type: "{{ item.vcenter_vm_type | default(omit) }}"
      vcenter_vnic_discovered: "{{ item.vcenter_vnic_discovered | default(omit) }}"
      vm_lb_weight: "{{ item.vm_lb_weight | default(omit) }}"
    with_items: "{{ avi_config.vimgrvmruntime | default([]) }}"

