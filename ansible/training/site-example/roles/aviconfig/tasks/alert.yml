# Automatically Generated File
# Copywrite Avi Networks

  - name: Avi Alert | Setup Alert
    avi_alert:
      controller: "{{ avi_controller}}"
      username: "{{ avi_username }}"
      password: "{{ avi_password }}"
      tenant: "{{ tenant | default(omit)}}"
      state: "{{item.state | default(omit)}}"
      api_version: "{{ api_version | default(omit)}}"
      name: "{{item.name}}"
      action_script_output: "{{ item.action_script_output | default(omit) }}"
      alert_config_ref: "{{ item.alert_config_ref | default(omit) }}"
      app_events: "{{ item.app_events | default(omit) }}"
      conn_events: "{{ item.conn_events | default(omit) }}"
      description: "{{ item.description | default(omit) }}"
      event_pages: "{{ item.event_pages | default(omit) }}"
      events: "{{ item.events | default(omit) }}"
      last_throttle_timestamp: "{{ item.last_throttle_timestamp | default(omit) }}"
      level: "{{ item.level | default(omit) }}"
      metric_info: "{{ item.metric_info | default(omit) }}"
      obj_key: "{{ item.obj_key | default(omit) }}"
      obj_name: "{{ item.obj_name | default(omit) }}"
      obj_uuid: "{{ item.obj_uuid | default(omit) }}"
      reason: "{{ item.reason | default(omit) }}"
      related_uuids: "{{ item.related_uuids | default(omit) }}"
      summary: "{{ item.summary | default(omit) }}"
      tenant_ref: "{{ item.tenant_ref | default(omit) }}"
      threshold: "{{ item.threshold | default(omit) }}"
      throttle_count: "{{ item.throttle_count | default(omit) }}"
      timestamp: "{{ item.timestamp | default(omit) }}"
      uuid: "{{ item.uuid | default(omit) }}"
    with_items: "{{ avi_config.alert | default([]) }}"

