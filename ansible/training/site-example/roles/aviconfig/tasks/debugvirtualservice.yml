# Automatically Generated File
# Copywrite Avi Networks

  - name: Avi DebugVirtualService | Setup DebugVirtualService
    avi_debugvirtualservice:
      controller: "{{ avi_controller}}"
      username: "{{ avi_username }}"
      password: "{{ avi_password }}"
      tenant: "{{ tenant | default(omit)}}"
      state: "{{item.state | default(omit)}}"
      api_version: "{{ api_version | default(omit)}}"
      name: "{{item.name}}"
      capture: "{{ item.capture | default(omit) }}"
      capture_params: "{{ item.capture_params | default(omit) }}"
      cloud_ref: "{{ item.cloud_ref | default(omit) }}"
      debug_hm: "{{ item.debug_hm | default(omit) }}"
      debug_ip: "{{ item.debug_ip | default(omit) }}"
      flags: "{{ item.flags | default(omit) }}"
      se_params: "{{ item.se_params | default(omit) }}"
      tenant_ref: "{{ item.tenant_ref | default(omit) }}"
      uuid: "{{ item.uuid | default(omit) }}"
    with_items: "{{ avi_config.debugvirtualservice | default([]) }}"

