# Automatically Generated File
# Copywrite Avi Networks

  - name: Avi VIMgrDCRuntime | Setup VIMgrDCRuntime
    avi_vimgrdcruntime:
      controller: "{{ avi_controller}}"
      username: "{{ avi_username }}"
      password: "{{ avi_password }}"
      tenant: "{{ tenant | default(omit)}}"
      state: "{{item.state | default(omit)}}"
      api_version: "{{ api_version | default(omit)}}"
      name: "{{item.name}}"
      cloud_ref: "{{ item.cloud_ref | default(omit) }}"
      cluster_refs: "{{ item.cluster_refs | default(omit) }}"
      host_refs: "{{ item.host_refs | default(omit) }}"
      interested_hosts: "{{ item.interested_hosts | default(omit) }}"
      interested_nws: "{{ item.interested_nws | default(omit) }}"
      interested_vms: "{{ item.interested_vms | default(omit) }}"
      inventory_state: "{{ item.inventory_state | default(omit) }}"
      managed_object_id: "{{ item.managed_object_id | default(omit) }}"
      nw_refs: "{{ item.nw_refs | default(omit) }}"
      pending_vcenter_reqs: "{{ item.pending_vcenter_reqs | default(omit) }}"
      sevm_refs: "{{ item.sevm_refs | default(omit) }}"
      tenant_ref: "{{ item.tenant_ref | default(omit) }}"
      type: "{{ item.type | default(omit) }}"
      uuid: "{{ item.uuid | default(omit) }}"
      vcenter_uuid: "{{ item.vcenter_uuid | default(omit) }}"
      vm_refs: "{{ item.vm_refs | default(omit) }}"
    with_items: "{{ avi_config.vimgrdcruntime | default([]) }}"

