# Automatically Generated File
# Copywrite Avi Networks

  - name: Avi CloudConnectorUser | Setup CloudConnectorUser
    avi_cloudconnectoruser:
      controller: "{{ avi_controller}}"
      username: "{{ avi_username }}"
      password: "{{ avi_password }}"
      tenant: "{{ tenant | default(omit)}}"
      state: "{{ avi_role_state_mode }}"
      api_version: "{{ api_version | default(omit)}}"
      name: "{{item.name}}"
      private_key: "{{ item.private_key | default(omit) }}"
      public_key: "{{ item.public_key | default(omit) }}"
      tenant_ref: "{{ item.tenant_ref | default(omit) }}"
      uuid: "{{ item.uuid | default(omit) }}"
    when:
      - (item.state | default("present") == avi_role_state_mode or avi_config_state == "absent")
    with_items: "{{ avi_config.cloudconnectoruser | default([]) }}"

