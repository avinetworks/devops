# Automatically Generated File
# Copywrite Avi Networks

  - name: Avi VIMgrVcenterRuntime | Setup VIMgrVcenterRuntime
    avi_vimgrvcenterruntime:
      controller: "{{ avi_controller}}"
      username: "{{ avi_username }}"
      password: "{{ avi_password }}"
      tenant: "{{ tenant | default(omit)}}"
      state: "{{item.state | default(omit)}}"
      api_version: "{{ api_version | default(omit)}}"
      name: "{{item.name}}"
      api_version: "{{ item.api_version | default(omit) }}"
      apic_mode: "{{ item.apic_mode | default(omit) }}"
      cloud_ref: "{{ item.cloud_ref | default(omit) }}"
      datacenter_refs: "{{ item.datacenter_refs | default(omit) }}"
      disc_end_time: "{{ item.disc_end_time | default(omit) }}"
      disc_start_time: "{{ item.disc_start_time | default(omit) }}"
      discovered_datacenter: "{{ item.discovered_datacenter | default(omit) }}"
      inventory_progress: "{{ item.inventory_progress | default(omit) }}"
      inventory_state: "{{ item.inventory_state | default(omit) }}"
      management_network: "{{ item.management_network | default(omit) }}"
      num_clusters: "{{ item.num_clusters | default(omit) }}"
      num_dcs: "{{ item.num_dcs | default(omit) }}"
      num_hosts: "{{ item.num_hosts | default(omit) }}"
      num_nws: "{{ item.num_nws | default(omit) }}"
      num_vcenter_req_pending: "{{ item.num_vcenter_req_pending | default(omit) }}"
      num_vms: "{{ item.num_vms | default(omit) }}"
      privilege: "{{ item.privilege | default(omit) }}"
      progress: "{{ item.progress | default(omit) }}"
      tenant_ref: "{{ item.tenant_ref | default(omit) }}"
      type: "{{ item.type | default(omit) }}"
      uuid: "{{ item.uuid | default(omit) }}"
      vcenter_connected: "{{ item.vcenter_connected | default(omit) }}"
      vcenter_fullname: "{{ item.vcenter_fullname | default(omit) }}"
      vcenter_template_se_location: "{{ item.vcenter_template_se_location | default(omit) }}"
      vcenter_url: "{{ item.vcenter_url | default(omit) }}"
    with_items: "{{ avi_config.vimgrvcenterruntime | default([]) }}"

