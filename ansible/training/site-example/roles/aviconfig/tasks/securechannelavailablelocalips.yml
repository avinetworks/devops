# Automatically Generated File
# Copywrite Avi Networks

  - name: Avi SecureChannelAvailableLocalIPs | Setup SecureChannelAvailableLocalIPs
    avi_securechannelavailablelocalips:
      controller: "{{ avi_controller}}"
      username: "{{ avi_username }}"
      password: "{{ avi_password }}"
      tenant: "{{ tenant | default(omit)}}"
      state: "{{item.state | default(omit)}}"
      api_version: "{{ api_version | default(omit)}}"
      name: "{{item.name}}"
      end: "{{ item.end | default(omit) }}"
      free_controller_ips: "{{ item.free_controller_ips | default(omit) }}"
      free_ips: "{{ item.free_ips | default(omit) }}"
      start: "{{ item.start | default(omit) }}"
      uuid: "{{ item.uuid | default(omit) }}"
    with_items: "{{ avi_config.securechannelavailablelocalips | default([]) }}"

