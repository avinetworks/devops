# Automatically Generated File
# Copywrite Avi Networks

  - name: Avi VIMgrHostRuntime | Setup VIMgrHostRuntime
    avi_vimgrhostruntime:
      controller: "{{ avi_controller}}"
      username: "{{ avi_username }}"
      password: "{{ avi_password }}"
      tenant: "{{ tenant | default(omit)}}"
      state: "{{item.state | default(omit)}}"
      api_version: "{{ api_version | default(omit)}}"
      name: "{{item.name}}"
      cloud_ref: "{{ item.cloud_ref | default(omit) }}"
      cluster_name: "{{ item.cluster_name | default(omit) }}"
      cluster_uuid: "{{ item.cluster_uuid | default(omit) }}"
      cntlr_accessible: "{{ item.cntlr_accessible | default(omit) }}"
      connection_state: "{{ item.connection_state | default(omit) }}"
      cpu_hz: "{{ item.cpu_hz | default(omit) }}"
      maintenance_mode: "{{ item.maintenance_mode | default(omit) }}"
      managed_object_id: "{{ item.managed_object_id | default(omit) }}"
      mem: "{{ item.mem | default(omit) }}"
      mgmt_portgroup: "{{ item.mgmt_portgroup | default(omit) }}"
      network_uuids: "{{ item.network_uuids | default(omit) }}"
      num_cpu_cores: "{{ item.num_cpu_cores | default(omit) }}"
      num_cpu_packages: "{{ item.num_cpu_packages | default(omit) }}"
      num_cpu_threads: "{{ item.num_cpu_threads | default(omit) }}"
      pnics: "{{ item.pnics | default(omit) }}"
      powerstate: "{{ item.powerstate | default(omit) }}"
      quarantine_start_ts: "{{ item.quarantine_start_ts | default(omit) }}"
      quarantined: "{{ item.quarantined | default(omit) }}"
      quarantined_periods: "{{ item.quarantined_periods | default(omit) }}"
      se_fail_cnt: "{{ item.se_fail_cnt | default(omit) }}"
      se_success_cnt: "{{ item.se_success_cnt | default(omit) }}"
      tenant_ref: "{{ item.tenant_ref | default(omit) }}"
      type: "{{ item.type | default(omit) }}"
      uuid: "{{ item.uuid | default(omit) }}"
      vm_refs: "{{ item.vm_refs | default(omit) }}"
    with_items: "{{ avi_config.vimgrhostruntime | default([]) }}"

