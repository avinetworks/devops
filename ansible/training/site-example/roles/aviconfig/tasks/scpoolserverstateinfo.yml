# Automatically Generated File
# Copywrite Avi Networks

  - name: Avi SCPoolServerStateInfo | Setup SCPoolServerStateInfo
    avi_scpoolserverstateinfo:
      controller: "{{ avi_controller}}"
      username: "{{ avi_username }}"
      password: "{{ avi_password }}"
      tenant: "{{ tenant | default(omit)}}"
      state: "{{item.state | default(omit)}}"
      api_version: "{{ api_version | default(omit)}}"
      name: "{{item.name}}"
      is_server: "{{ item.is_server | default(omit) }}"
      oper_status: "{{ item.oper_status | default(omit) }}"
      pool_ref: "{{ item.pool_ref | default(omit) }}"
      server_states: "{{ item.server_states | default(omit) }}"
      tenant_ref: "{{ item.tenant_ref | default(omit) }}"
      uuid: "{{ item.uuid | default(omit) }}"
    with_items: "{{ avi_config.scpoolserverstateinfo | default([]) }}"

